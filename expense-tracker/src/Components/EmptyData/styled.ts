import { Empty } from "antd";
import styled from "styled-components";

export const StyledEmptyData = styled(Empty)`
  position: absolute;
  left: 50%;
  transform: translate(-50%, -50%);
  top: 50%;
`;
